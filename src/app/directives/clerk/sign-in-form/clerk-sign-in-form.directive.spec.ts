import { ClerkSignInFormDirective } from './clerk-sign-in-form.directive';

describe('ClerkSignInFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ClerkSignInFormDirective();
    expect(directive).toBeTruthy();
  });
});

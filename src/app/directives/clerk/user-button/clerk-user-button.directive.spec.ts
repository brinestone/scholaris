import { ClerkUserButtonDirective } from './clerk-user-button.directive';

describe('UserButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ClerkUserButtonDirective();
    expect(directive).toBeTruthy();
  });
});

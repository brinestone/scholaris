import { ClerkSignUpDirective } from './clerk-sign-up.directive';

describe('ClerkSignUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ClerkSignUpDirective();
    expect(directive).toBeTruthy();
  });
});
